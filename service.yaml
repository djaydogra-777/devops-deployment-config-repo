apiVersion: v1
kind: Service
metadata:
  name: simple-react-app
  labels:
    app: simple-react-app
spec:
  type: LoadBalancer
  selector:
    app: simple-react-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80

 